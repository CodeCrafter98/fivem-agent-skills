# Google Antigravity

Workspace skills: `.agents/skills/<skill>/SKILL.md`. Persistent workspace instructions can use `.agents/AGENTS.md`. For user-global compatibility, the installer can also copy skills to `~/.gemini/config/skills/`.
